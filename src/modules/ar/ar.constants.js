const AR_STATUS = Object.freeze({
  PENDING: 'PENDING',
  INVOICE_NOT_FOUND: 'INVOICE_NOT_FOUND',
  MISSING_DOCUMENTS: 'MISSING_DOCUMENTS',
  READY_FOR_PDF: 'READY_FOR_PDF',
  PDF_GENERATED: 'PDF_GENERATED',
  EMAIL_SENT: 'EMAIL_SENT',
});

const AR_JOB_NAMES = Object.freeze({
  DAILY: 'AR_PROCESSING_JOB',
  PROCESS_TARGET: 'AR_PROCESS_TARGET',
});

const PDF_JOB_NAMES = Object.freeze({
  CREATE: 'PDF_CREATION_JOB',
});

const EMAIL_JOB_NAMES = Object.freeze({
  SEND: 'SEND_EMAIL_JOB',
});

const AR_LOG_LEVEL = Object.freeze({
  INFO: 'info',
  WARN: 'warn',
  ERROR: 'error',
});

const CONFIDENCE_THRESHOLD = 0.5;
const DEFAULT_INVOICE_DATE_PROXIMITY_DAYS = 3;

module.exports = {
  AR_STATUS,
  AR_JOB_NAMES,
  PDF_JOB_NAMES,
  EMAIL_JOB_NAMES,
  AR_LOG_LEVEL,
  CONFIDENCE_THRESHOLD,
  DEFAULT_INVOICE_DATE_PROXIMITY_DAYS,
};
